// Placeholder shell view: proves the window boots and loads the renderer
// bundle. Real content (local stats, queue status) arrives in Task 4.
export function App() {
  return (
    <div>
      <h1>TokenOps</h1>
      <p>Agent running</p>
    </div>
  );
}
